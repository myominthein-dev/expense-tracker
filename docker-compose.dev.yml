version: "3.9"

services:
  vue-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: vue3_dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
